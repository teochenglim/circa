# v1.0.0 — Hardening and packaging

Status: **Not started.**

## Planned scope

- Sizing calculator (metric count × resolution × retention → disk budget), per [DESIGN/03](../DESIGN/03_design_storage.md) §3.3.
- Packaging: systemd unit, Docker image, Helm chart — the `Dockerfile`, `k8s/`, and `helm/` scaffolding already in this repo should have real code to build against by this point.
- Docs pass: explicit resource-cost notes per feature flag (§8.1.3), so enabling ML/alerts/backup is an informed choice.
- Resolve as many of [DESIGN/09](../DESIGN/09_design_tech_stack_and_roadmap.md) §9.2's open questions as still apply.
- **UI polish pass, last**: the v0.6.0 tabbed dashboard (Overall + per-category pages) and its category-color design system already ship real data end to end — this is refinement, not new functionality, which is why it's the final item here rather than its own milestone. Concretely: visualize circa's own RED self-metrics (`circa_http_requests_total`/`_duration_seconds` and every other package's self-metrics added in v0.7.0 — see [ARCHITECTURE.md](../ARCHITECTURE.md)) somewhere in the dashboard, finally giving DESIGN/05's "lightweight self-metrics panel" a real UI, not just a `/metrics` text endpoint; a general dark/light pass across every v0.6.0 page once there's a full milestone of real usage to learn from what needs tightening.

  (This replaces an earlier draft of this section that specified a mock dashboard with **randomized/simulated** metric values under a "Node Exporter Full" title — worth flagging explicitly: that's the opposite of what this project is. Circa's dashboard renders real data from `internal/query`/`/metrics`, never fake or randomly-generated numbers, and it's Circa's own UI, not node_exporter's. If that draft came from somewhere else, it doesn't belong in this repo's plans as written.)

## Built

Not yet — nothing has shipped.
